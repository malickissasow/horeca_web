name: Deploy Web to app.horecafrica.org (Hostinger FTP)

on:
  push:
    branches: [ main, production ]
    paths:
      - 'src/**'
      - 'public/**'
      - 'index.html'
      - 'package.json'
      - 'package-lock.json'
      - 'vite.config.ts'
      - '.github/workflows/**'
      - '.github/scripts/**'
  workflow_dispatch:

jobs:
  deploy-app-horecafrica:
    name: 🚀 Isolated Deploy to app.horecafrica.org
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm install

      - name: Build production bundle
        run: npm run build
        env:
          VITE_API_URL: https://api.horecafrica.org/api

      - name: Verify build output
        run: |
          if [ ! -f "dist/index.html" ]; then
            echo "❌ Build failed: dist/index.html not found"
            exit 1
          fi
          echo "✅ Build successful! dist directory ready."

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📤 FTP Deploy Web Frontend to Hostinger
        run: python3 deploy_web_ftp.py
        env:
          FTP_HOST: '92.113.24.18'
          FTP_PORT: '21'
          FTP_USER: 'u208608546.app.horecafrica.org'
          FTP_PASS: 'B5@9ll@c'

      - name: Deployment Summary
        run: echo "🎉 Application deployed strictly to https://app.horecafrica.org"
